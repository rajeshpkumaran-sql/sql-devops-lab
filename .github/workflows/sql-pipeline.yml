name: SQL DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  sql-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show files
        run: ls -R

      - name: Display SQL file
        run: cat sql/create_tables.sql